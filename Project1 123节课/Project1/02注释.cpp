#include<iostream>
using namespace std;//调用std这个库，后面就不需要额外加std了
//要不然就要每次调用都要输入（std：：）

int main2()
{
	/*
	main是一个程序的入口
	每个程序都必须有这么个函数
	有且仅有一个（其它有的话就要重命名）
	*/

	//11行是输出这一串字符
	cout << "hello world1" << endl;

	system("pause");

	return 0;
}